/* const jugarPiedraPapelTijera = (a, b) =>{
    const empate = (a === b);

    const ganaUsuarioA = (a === "piedra" && b === "tijera") || (a === "papel" && b === "piedra") || (a === "tijera" && b === "papel")
    
    const ganaUsuarioB = (b === "piedra" && a === "tijera") || (b === 
    "papel" && a === "piedra") || (b === "papel" && a === "roca") 
} */